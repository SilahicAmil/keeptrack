package azuredevops

import "changeme/internal/services/models"

// Fetch PRs for ticket(s), reviewers, threads

func (c *AzureDevopsClient) FetchActivePRs(user *models.CurrentUser) ([]models.PullRequest, error) {
	return nil, nil
}
